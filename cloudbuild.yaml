steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - hello_world
  - --region=us-central1
  - --source=.
  - --trigger-http
  - --runtime=python310
  dir: 'hello_world'

options:
  logging: CLOUD_LOGGING_ONLY
